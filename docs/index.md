# Real Estate Heterogeneous Data Analytics — Documentation

Welcome. Use the left navigation to browse:

1. **Pipeline tour** — End-to-end flow in seven chapters (aligned with `PipelineService.execute()`).
2. **Architecture (from source tree)** — The same `ARCHITECTURE.md` files that live next to the code (included here so MkDocs can render them).
3. **Project README** — Repository overview from the root `README.md`.

Diagrams use **Mermaid**; they render in the Material theme when you run `mkdocs serve` or open files under `site/` after `mkdocs build`.
